function [] = unitConverter()
    global gui;
    
    %Needs: 2 radio button sets, one for the input and one for the desired
    %conversion, each with a callback to a conversion function, 1 editable 
    %text input string, 1 output text (non-editable), 1 pushbutton to
    %display the conversion.
   
    
end